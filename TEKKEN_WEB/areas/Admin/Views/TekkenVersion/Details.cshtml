@model TekkenVersion

@{
    Layout = "_Layout";
}

<h2 style="text-align:center;">버전</h2>
<span style="color: #ff0000">버전 수정 - 현재 글에 대해서 수정 및 삭제를 할 수 있습니다.</span>
<hr />

<table style="width: 700px; margin-left: auto; margin-right: auto;">
    <tbody>
        <tr style="color: white; background-color: #46698c;">
            <td style="width: 80px; text-align: right; height: 35px;">
                <b style="font-size: 18px">@Html.DisplayNameFor(model => model.Version)</b> :
            </td>
            <td>
                <span style="font-weight:bold;font-size:18px;">
                    @Model.Version
                </span>
            </td>
        </tr>

        <tr style="background-color: #efefef;">
            <td class="text-right">
                @Html.DisplayNameFor(model => model.Season)
            </td>
            <td>
                <span>@Model.Season</span>
            </td>
        </tr>
        <tr style="background-color: #efefef;">
            <td class="text-right">
                @Html.DisplayNameFor(model => model.UpdateDate)
            </td>
            <td>
                <span>@Model.Version</span>
            </td>
        </tr>
    </tbody>
</table>



<div style="text-align:center;">

    <a asp-action="Remove" asp-route-version="@Model.Version" class="btn btn-default">
        삭제
    </a>
    <a asp-action="Update" asp-route-version="@Model.Version" class="btn btn-default">
        수정
    </a>
    <a asp-action="Remove" asp-route-version="@Model.Version" class="btn btn-default">
        답변
    </a>

    <a asp-action="Index" class="btn btn-default">리스트</a>
</div>
<br />

@section Scripts {
    @if (TempData["Message"] != null)
    {
        <link href="~/lib/toastr/toastr.css" rel="stylesheet" />
        <script src="~/lib/toastr/toastr.js"></script>
        <script>
            $(function () {
                toastr.info('@TempData["Message"]');
            });
        </script>
    }
}