@page "/Components/Base/Name/CreateNameComponent"
@inherits BaseComponent<TDataEntity, TNameEntity>
@typeparam TDataEntity
@typeparam TNameEntity

<h3>CreateNameComponent</h3>
@if (BaseNameEntity == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <form>
        <div class="form-group row">
            <label class="col-sm-1 col-form-label">ID</label>
            <div class="col-sm-10">
                <input type="text" readonly class="form-control" @bind-value="@BaseNameEntity.Id" />
            </div>
        </div>

        <div class="form-group row">
            <label class="col-sm-1 col-form-label">코드</label>
            <div class="col-sm-10">
                <input type="text" readonly class="form-control" @bind-value="@BaseNameEntity.Base_code" />
            </div>
        </div>

        <div class="form-group row">
            <label class="col-sm-1 col-form-label">언어</label>
            <div class="col-sm-10">
                <input type="text" readonly class="form-control" @bind-value="@BaseNameEntity.Language_code" />
            </div>
        </div>

        <div class="form-group row">
            <label class="col-sm-1 col-form-label">명칭</label>
            <div class="col-sm-10">
                <input type="text" class="form-control" @bind-value="@BaseNameEntity.Name" />
            </div>
        </div>
        <div class="form-group">
            <input type="button" class="btn btn-primary" value="저장" id="btnEdit_Click" @onclick="SaveCreate" />
            <input type="button" class="btn btn-primary" value="리스트" id="btnList_Click" @onclick="MoveToList" />
        </div>
    </form>
}
