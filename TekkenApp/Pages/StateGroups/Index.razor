@page "/StateGroups"
@page "/StateGroups/Index"

<h1 class="text-center">StateGroups</h1>
<div>
    <a href="/StateGroups/Create" class="btn btn-primary">추가</a>
</div>


<style>
    .table tbody tr:nth-child(4n-3) {
        background: #e2e2e2;
    }
</style>

<div class="container">
    <div class="m-auto" id="Character-list">
        <div class="table-responsive">
            @if (stateGroups == null)
            {
                <p><em>Loading...</em></p>
            }
            else
            {
                <table class="table table-bordered border-primary" style="width: 100%; margin-left: auto; margin-right: auto; border: 0;">
                    <colgroup>
                        <col style="width: 100px;" />
                        <col style="width: 100px;" />
                        <col style="width: 150px;" />
                    </colgroup>
                    <thead class="table-dark">
                        <tr>
                            <th>CODE</th>
                            <th>NUMBER</th>
                            <th>DESCRIPTION</th>
                            <th>MODIFY</th>
                        </tr>
                    </thead>

                    <tbody>
                        @foreach (var stateGroup in stateGroups)
                        {
                            <tr id="@stateGroup.Code">
                                <td>@stateGroup.Code</td>
                                <td>@stateGroup.Number</td>
                                <td>@stateGroup.Description</td>
                                <td>
                                    @*<a asp-action="/HitTypes/Details_name/" class="btn btn-primary" asp-route-id="@hitType.Id">수정</a>*@
                                    <a href="/StateGroups/Edit/@stateGroup.Id">수정</a>
                                    @*<a href="@hitType_name.Id">상세</a>*@
                                    <!--<a asp-action="UpdateAllName" class="btn btn-primary" asp-route-id="@hitType.Id">명칭 편집</a>-->
                                </td>
                            </tr>

                            foreach (var stateGroup_name in stateGroupService.GetStateGroup_AllTranslateNamesByCodeAsync(stateGroup.Code).Result.ToList())
                            {
                                <tr>
                                    <td>@stateGroup_name.Language_code</td>
                                    <td colspan="2">
                                        @if (String.IsNullOrEmpty(stateGroup_name.Name))
                                        {
                                            <a asp-action="CreateName" class="btn btn-primary" asp-route-code="@stateGroup_name.Base_code" asp-route-languageCode=@stateGroup_name.Language_code asp-route-name="@stateGroup_name">추가</a>
                                        }
                                        else
                                        {
                                            @stateGroup_name.Name
                                        }
                                    </td>
                                    <td>
                                        <a href="/StateGroups/Details_name/@stateGroup_name.Id">상세</a>
                                        <a href="/StateGroups/Edit_name/@stateGroup_name.Id">편집</a>
                                        @*<a asp-action="UpdateName" class="btn btn-primary" asp-route-id="@hitType_name.Id">명칭 편집</a>*@
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            }
        </div>
    </div>
</div>


@*                    {
    <tr>
        <td>
            @Html.Raw(@item2.Language_Code)
        </td>
        <td colspan="2">
            @if (String.IsNullOrEmpty(@item2.Name))
            {
                <a asp-action="CreateName" class="btn btn-primary" asp-route-code="@item.Code" asp-route-languageCode=@item2.Language_Code asp-route-name="@Html.Raw(@item.Description)">추가</a>
            }
            else
            {
                @Html.Raw(@item2.Name)
            }
        </td>
        <td>
            <a asp-action="UpdateName" class="btn btn-primary" asp-route-id="@item2.Id">명칭 편집</a>
        </td>
    </tr>
                            }*@
                    }